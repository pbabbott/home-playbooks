---
# Install and enable QEMU guest agent (play 1 of configure-vm).

- name: Install and enable QEMU guest agent
  hosts: ansible-template-ubuntu-noble
  tasks:
    - name: Install qemu-guest-agent
      ansible.builtin.apt:
        name: qemu-guest-agent
        state: present
        update_cache: true
      become: true
