- name: Traefik configuration for handbrake
  hosts: bananapi
  roles:
    - role: home.traefik_app
      traefik_app_file_name: 'plex.yml'

    - role: home.traefik_app
      traefik_app_file_name: 'portal.nas.local.yml'

    - role: home.traefik_app
      traefik_app_file_name: 'portal.nas.local.yml'
