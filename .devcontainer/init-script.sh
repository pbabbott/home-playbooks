# Set up git origin to resolve push warning
git config --global push.autoSetupRemote true

# Use project .zshrc so it's respected in every new shell
# SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
# ln -sf "${SCRIPT_DIR}/.zshrc" "${HOME}/.zshrc"
